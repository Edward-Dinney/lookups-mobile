# URL of the deployed Netlify function that mints Spotify access tokens.
# The Spotify client secret lives there, never in this app — anything in an
# Expo bundle can be extracted from the shipped .ipa/.aab.
EXPO_PUBLIC_SPOTIFY_TOKEN_ENDPOINT=https://your-site-name.netlify.app/api/spotify-token
